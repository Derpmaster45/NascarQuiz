using System;

namespace NascarQuiz
{
    class Game
    {
        static void Main(string[] args)
        {
            Questions questionBank = new Questions();
            bool isRunning = true;
            // scoring variables
            int numCorrect = 0;
            int numIncorrect = 0;
            while (isRunning) 
            {
                Questions.init();
                
            }
        }
    }
}
